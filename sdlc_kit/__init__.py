"""sdlc-kit: a small, harness-neutral AI-native SDLC you can drop into any git project.

`pip install sdlc-kit` exposes the installer as the `sdlc-kit-install` console script; the same code is at
`sdlc_kit.install.main` for programmatic use. A git clone uses the top-level `install.py` shim, which re-exports the
same entry point.
"""

__version__ = "0.1.0"
__author__ = "sdlc-kit maintainers"
__license__ = "Apache-2.0"