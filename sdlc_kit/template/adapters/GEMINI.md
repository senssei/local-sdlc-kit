@AGENTS.md

## Gemini CLI specifics

- The process and the project rules are in `AGENTS.md` (imported above). The stage skills are in `.agents/skills/<name>/SKILL.md`:
  read `.agents/skills/sdlc/SKILL.md` first, it finds the stage from `plan.md` and git.
- For the review stage, the independent reviewer that `AGENTS.md` requires is a new session or a subagent.
